import React from "react";
import { createRoot } from "react-dom/client";
import CaremapsPrototype from "./CaremapsPrototype.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CaremapsPrototype />
  </React.StrictMode>
);
